package fr.cotedazur.univ.polytech.ttr.equipeb.models.cards;

/**
 * Enum representing the available colors for Wagon cards.
 */
public enum CardColor {
    VIOLET, WHITE, BLUE, YELLOW, ORANGE, BLACK, RED, GREEN, MULTICOLOR
}