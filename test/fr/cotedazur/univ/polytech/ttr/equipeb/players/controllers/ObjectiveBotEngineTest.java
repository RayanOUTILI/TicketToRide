package fr.cotedazur.univ.polytech.ttr.equipeb.players.controllers;

import fr.cotedazur.univ.polytech.ttr.equipeb.models.cards.DestinationCard;
import fr.cotedazur.univ.polytech.ttr.equipeb.models.map.City;
import org.junit.jupiter.api.Test;

import java.util.List;

import static org.junit.jupiter.api.Assertions.*;

class ObjectiveBotEngineTest {

    /*
    @Test
    void findDestinationsWithMostCommonCities() {
        City city1 = new City("city1");
        City city2 = new City("city2");
        City city3 = new City("city3");
        City city4 = new City("city4");
        City city = new City("city");

        DestinationCard card1 = new DestinationCard(city1, city2, 10);
        DestinationCard card2 = new DestinationCard(city, city3, 10);

        List<DestinationCard> cards = List.of(card1, card2);
        ObjectiveBotEngine engine = new ObjectiveBotEngine(null, null, null);
        List<DestinationCard> pair = engine.findDestinationsWithMostCommonCities(cards);
        System.out.println(pair.get(0).getCities());
        System.out.println(pair.get(1).getCities());
    }
    */
}